var assert = require('chai').assert

describe('lint-examples', () => {
  describe('valid example', () => {
    it('should be valid')
  })
  describe('valid example with yaml instead of yml', () => {
    it('should be valid')
  })
  describe('invalid example', () => {
    it('should be invalid')
  })
  describe('custom paths', () => {
    it('should work')
  })
})